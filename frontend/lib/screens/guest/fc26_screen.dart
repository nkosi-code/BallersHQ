import 'package:flutter/material.dart';

class FC26Screen extends StatelessWidget {
  const FC26Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("FC26"),
      ),
      body: const Center(
        child: Text(
          "🚧 UNDER CONSTRUCTION 🚧",
          style: TextStyle(fontSize: 22),
        ),
      ),
    );
  }
}